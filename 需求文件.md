
主要做两个表
1.任务表
主要有 
任务类别(接口方法名) 
任务开始newsid
任务执行中newsid
任务结束newsid或者结束时间(可两个字段 用于历史同步)
任务执行状态 
任务创建时间 更新时间(中国时区) 
2.数据表 用于存储新闻数据 同时增加 数据获取方式(接口方法名)

四个主要任务 做成单独的服务单独的py文件
1.实时获取无头浏览器数据
通过无头浏览器前端获取实时数据
2.api获取实时数据
3.api获取历史数据
4.登陆状态检测 

主业务机器人
机器人用于检查任务 和监督任务执行情况
1.(空库)
	如今天是5月23日 开启了同步至5月1日
	1.开启一次实时任务 获取最新的newsid(根据配置 如果api和前端同时开启 优先api)
	2.数据存入任务表的3个任务中
		1.api实时(存入开始newsid ,任务执行中newsid)
		2.网页实时(同上)
		3.历史任务(开始newsid,结束时间)
	
2(有未完成的历史任务)
	如今天是5月23日 开启了同步至5月1日 但是数据库中已经有 22和21日的数据
	1.首先获取到最新的一条数据(同上空库)
	 2.生成一个历史同步数据任务
		当前最小newsid 最近时间(22日)最大的newsid
		
3.任务分发和检测
	1.任务发给方法
	2.历史数据进度检测
		1.方法回传 如果获取到的新闻时间已经小于5月1日 那么方法自动停止
		2.主任务定时检测:检查任务执行的进度 如果任务已经判定完成 则更新数据库 并结束任务
	3.历史任务如果有未完成的 优先执行最近的
	4.登陆状态检测 如果掉线了 暂停所有任务 等待登陆完成后才继续任务(禁止未登录获取数据) 
	5.方法存活检测 定时检测方法存活 如果死了 杀死进程并重发任务(更新进度)
	
4.做一个附加任务  对主任务进程的存活进行检测 如果死了就重启
可设置该方法定时重启时间写入配置文件 本方法默认1个小时重启一次 